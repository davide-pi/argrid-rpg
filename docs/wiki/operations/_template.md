<!-- Copy this file to <kebab-case-title>.md, fill it in, then add a row to ../README.md "Index — operations". -->
# <Operation name — what it does>

- **Purpose:** one line — when and why you run this.
- **Applies to:** <environment(s) / part of the app>
- **Risk:** low | medium | high — what can go wrong.
- **Last verified:** <YYYY-MM-DD>

## Prerequisites

Access, tools, versions, env needed before starting.

## Steps

Numbered, copy-pastable. Every command / query / script in a fenced, language-tagged block.

1. <step>
   ```bash
   # command for this step
   ```

## Verify

How to confirm it worked (expected output / state).

## Rollback / if it fails

How to undo, or what to do when a step fails.
