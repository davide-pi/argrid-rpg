/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Injected at build time by Vite (see vite.config.ts) — the app version
// computed by GitVersion in CI, shown as the on-map version badge.
declare const __APP_VERSION__: string;
